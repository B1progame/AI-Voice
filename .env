# =========================
# App / Server
# =========================
APP_HOST=127.0.0.1
APP_PORT=8000
APP_ENV=dev

# Cookie settings (when behind HTTPS via Caddy, set true)
COOKIE_SECURE=false
COOKIE_SAMESITE=lax

# JWT
JWT_SECRET=CHANGE_ME_TO_A_LONG_RANDOM_SECRET
JWT_EXPIRE_MINUTES=10080

# =========================
# Admin bootstrap (auto-created on first start if not exists)
# =========================
ADMIN_EMAIL=admin@example.local
ADMIN_PASSWORD=B1pro!

# =========================
# Ollama (running in WSL2, reachable from Windows host via localhost)
# Default port is often 11434.
# =========================
OLLAMA_URL=http://localhost:11435
OLLAMA_MODEL=llama3.1:8b

# Optional: a short system prompt used for every chat
SYSTEM_PROMPT=You are a helpful assistant.

# =========================
# Caddy / LAN domain (used in Caddyfile via env var)
# You can use a LAN hostname or an IP (recommended: host name).
# Examples:
#   APP_DOMAIN=aiassistant.local
#   APP_DOMAIN=192.168.178.20
# =========================
APP_DOMAIN=aiassistant.local